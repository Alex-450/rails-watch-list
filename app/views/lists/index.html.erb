<div class="container">
  <div class="row">
    <div class="col">
      <h1>list index</h1>

      <% @lists.each do |list| %>
        <%= link_to list.name, list_path(list) %>
      <% end %>

      <%= link_to 'create new list', new_list_path(), class: 'btn btn-primary' %>
    </div>
  </div>
</div>
